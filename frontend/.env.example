# Backend origin only, without /api/v1. The API service appends that path.
# Local development example. In Vercel, set this to Ani's publicly reachable HTTPS backend origin.
VITE_API_BASE_URL=http://127.0.0.1:8000
